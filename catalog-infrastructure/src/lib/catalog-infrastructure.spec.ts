import { catalogInfrastructure } from './catalog-infrastructure';

describe('catalogInfrastructure', () => {
  it('should work', () => {
    expect(catalogInfrastructure()).toEqual('catalog-infrastructure');
  });
});
